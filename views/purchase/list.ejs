<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Satın Alma Siparişleri — Enterprise ERP</title>
  <link rel="stylesheet" href="/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body class="module-body">

  <!-- Module Sidebar -->
  <aside class="module-sidebar">
    <div class="module-sidebar-brand">
      <a href="/" class="msb-logo">
        <svg width="28" height="28" viewBox="0 0 32 32" fill="none"><rect width="32" height="32" rx="8" fill="url(#bGrad)"/><path d="M8 12h16M8 16h12M8 20h8" stroke="#fff" stroke-width="2.2" stroke-linecap="round"/><defs><linearGradient id="bGrad" x1="0" y1="0" x2="32" y2="32"><stop offset="0%" stop-color="#6366f1"/><stop offset="100%" stop-color="#8b5cf6"/></linearGradient></defs></svg>
        <span>Enterprise ERP</span>
      </a>
    </div>

    <div class="module-sidebar-section">
      <div class="mss-label">🛒 SATIN ALMA YÖNETİMİ</div>

      <div class="mss-group-title">📊 DASHBOARD & ONAY</div>
      <div class="mss-sub">
        <a href="/purchase/analytics" class="mss-sub-link">📊 Satın Alma Analitiği</a>
        <a href="/purchase/approvals" class="mss-sub-link">⚡ Yönetsel Onaylar</a>
      </div>

      <div class="mss-group-title">📋 SATIN ALMA OPERASYONLARI</div>
      <div class="mss-sub">
        <a href="/purchase/requisitions" class="mss-sub-link">📩 Satın Alma Talepleri</a>
        <a href="/purchase/rfq" class="mss-sub-link">📨 Teklif Yönetimi (RFQ)</a>
        <a href="/purchase/orders" class="mss-sub-link active">📦 Satın Alma Siparişleri</a>
        <a href="/purchase/goods-receipt" class="mss-sub-link">🚛 Mal Kabul (GRN)</a>
      </div>

      <div class="mss-group-title">🏭 TEDARİKÇİ YÖNETİMİ</div>
      <div class="mss-sub">
        <a href="/purchase/suppliers" class="mss-sub-link">🏢 Tedarikçi Kartları</a>
      </div>
    </div>

    <!-- Sidebar User -->
    <div class="module-sidebar-user">
      <div class="msu-avatar"><%= user.username.charAt(0).toUpperCase() %></div>
      <div class="msu-meta">
        <strong><%= user.firstName ? user.firstName + ' ' + user.lastName : user.username %></strong>
        <span><%= user.role %></span>
      </div>
    </div>
  </aside>

  <!-- Main Area -->
  <div class="module-main">
    <!-- Top Navbar -->
    <header class="module-topbar">
      <div class="mt-left">
        <a href="/" class="mt-link">Ana Sayfa</a>
        <% if (user.role === 'Admin') { %>
          <a href="/admin" class="mt-link">Sistem Yönetimi</a>
        <% } %>
        <a href="/stock" class="mt-link">Stok & Depo</a>
        <a href="/sales" class="mt-link">Satış Yönetimi</a>
        <a href="/purchase" class="mt-link active">Satın Alma</a>
        <% if (user.role === 'Admin' || user.role === 'Production_Manager') { %>
          <a href="/production" class="mt-link">Üretim Planlama</a><a href="/quality" class="mt-link">Kalite Kontrol</a>
        <% } %>
      </div>
      <div class="mt-right">
        <span class="mt-time" id="liveTime"></span>
        <a href="/logout" class="mt-logout" title="Çıkış">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
        </a>
      </div>
    </header>

    <!-- Content -->
    <main class="module-content">
      <!-- Sub-Nav Bar -->
      <div class="module-subnav">
        <a href="/purchase/analytics" class="msn-link">📊 Analitik</a>
        <a href="/purchase/approvals" class="msn-link">⚡ Onaylar</a>
        <a href="/purchase/requisitions" class="msn-link">📩 Talepler</a>
        <a href="/purchase/rfq" class="msn-link">📨 RFQ</a>
        <a href="/purchase/orders" class="msn-link active">📦 Siparişler</a>
        <a href="/purchase/goods-receipt" class="msn-link">🚛 Mal Kabul</a>
        <a href="/purchase/suppliers" class="msn-link">🏢 Tedarikçiler</a>
      </div>

      <div class="page-header">
        <div>
          <h1>Satın Alma Siparişleri (PO)</h1>
          <p style="color: var(--text-secondary); font-size: 0.9rem; margin-top: 4px;">Tedarikçi siparişlerini, hammadde girdilerini ve alış bütçenizi yönetin.</p>
        </div>
        <a href="/purchase/rfq" class="btn btn-secondary" style="border-radius: 10px;">
          📨 RFQ Teklif Yönetimine Git
        </a>
      </div>

      <% if (typeof successMsg !== 'undefined' && successMsg) { %>
        <div style="background: rgba(16, 185, 129, 0.15); color: #34d399; border: 1.5px solid rgba(16, 185, 129, 0.35); padding: 16px 22px; border-radius: 16px; margin-bottom: 24px; font-weight: 700; display: flex; align-items: center; gap: 12px; box-shadow: 0 8px 25px rgba(16, 185, 129, 0.15);">
          <span style="font-size: 1.3rem;">✅</span> <%= successMsg %>
        </div>
      <% } %>

      <!-- Purchase KPI Strip -->
      <div class="kpi-strip">
        <div class="kpi-chip">
          <div class="kpi-chip-icon kpi-chip--indigo">📦</div>
          <div class="kpi-chip-data">
            <span class="kpi-chip-label">Toplam Satın Alma</span>
            <span class="kpi-chip-value"><%= stats.totalOrders %></span>
          </div>
        </div>
        <div class="kpi-chip">
          <div class="kpi-chip-icon kpi-chip--amber">⏳</div>
          <div class="kpi-chip-data">
            <span class="kpi-chip-label">Onay Bekleyen</span>
            <span class="kpi-chip-value"><%= stats.pendingOrders %></span>
          </div>
        </div>
        <div class="kpi-chip">
          <div class="kpi-chip-icon kpi-chip--emerald">🚚</div>
          <div class="kpi-chip-data">
            <span class="kpi-chip-label">Teslim Alınan</span>
            <span class="kpi-chip-value"><%= stats.receivedOrders %></span>
          </div>
        </div>
        <div class="kpi-chip">
          <div class="kpi-chip-icon kpi-chip--rose">💳</div>
          <div class="kpi-chip-data">
            <span class="kpi-chip-label">Toplam Harcama</span>
            <span class="kpi-chip-value"><%= parseFloat(stats.totalSpend).toLocaleString('tr-TR', { minimumFractionDigits: 2 }) %> ₺</span>
          </div>
        </div>
      </div>

      <!-- Filters -->
      <div class="filter-bar">
        <form method="GET" action="/purchase/orders" class="filter-form">
          <div class="filter-input-wrap">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
            <input type="text" name="search" placeholder="Sipariş no, tedarikçi unvanı veya vergi no..." value="<%= filterSearch %>" class="filter-input">
          </div>
          <select name="status" class="filter-select" onchange="this.form.submit()">
            <option value="">Tüm Sipariş Durumları</option>
            <option value="Draft" <%= filterStatus === 'Draft' ? 'selected' : '' %>>Taslak</option>
            <option value="Pending_Approval" <%= filterStatus === 'Pending_Approval' ? 'selected' : '' %>>Onay Bekliyor</option>
            <option value="Ordered" <%= filterStatus === 'Ordered' ? 'selected' : '' %>>Sipariş Verildi</option>
            <option value="Partial_Received" <%= filterStatus === 'Partial_Received' ? 'selected' : '' %>>Kısmi Teslim Alındı</option>
            <option value="Received" <%= filterStatus === 'Received' ? 'selected' : '' %>>Teslim Alındı</option>
            <option value="Cancelled" <%= filterStatus === 'Cancelled' ? 'selected' : '' %>>İptal</option>
          </select>
          <select name="paymentTerm" class="filter-select" onchange="this.form.submit()">
            <option value="">Tüm Ödeme Koşulları</option>
            <option value="Pesin" <%= filterPaymentTerm === 'Pesin' ? 'selected' : '' %>>Peşin</option>
            <option value="Vadeli_30" <%= filterPaymentTerm === 'Vadeli_30' ? 'selected' : '' %>>30 Gün Vadeli</option>
            <option value="Vadeli_60" <%= filterPaymentTerm === 'Vadeli_60' ? 'selected' : '' %>>60 Gün Vadeli</option>
            <option value="Vadeli_90" <%= filterPaymentTerm === 'Vadeli_90' ? 'selected' : '' %>>90 Gün Vadeli</option>
            <option value="Kredi_Karti" <%= filterPaymentTerm === 'Kredi_Karti' ? 'selected' : '' %>>Kredi Kartı</option>
          </select>
          <button type="submit" class="btn btn-sm">Filtrele</button>
        </form>
      </div>

      <!-- Results Count -->
      <div class="results-info">
        <span>Toplam <strong><%= orders.length %></strong> satın alma siparişi listeleniyor</span>
      </div>

      <!-- Purchase Table -->
      <div class="table-wrapper">
        <table class="data-table">
          <thead>
            <tr>
              <th>Sipariş No</th>
              <th>Tedarikçi Unvanı</th>
              <th>Sipariş Edilen Ürünler</th>
              <th>Miktar</th>
              <th>Toplam İskonto</th>
              <th>Genel Toplam</th>
              <th>Ödeme Koşulu</th>
              <th>Tarih</th>
              <th>Durum</th>
              <th>Aksiyon</th>
            </tr>
          </thead>
          <tbody>
            <% if (orders.length === 0) { %>
              <tr>
                <td colspan="10" style="text-align: center; padding: 3rem; color: var(--text-muted);">
                  <div style="font-size: 2rem; margin-bottom: 8px;">🏭</div>
                  Henüz kayıtlı satın alma siparişi bulunmamaktadır.
                  <br><a href="/purchase/rfq" style="color: var(--indigo); font-weight: 600;">RFQ Teklif Yönetiminden sipariş oluşturabilirsiniz →</a>
                </td>
              </tr>
            <% } else { %>
              <% orders.forEach(order => { 
                let itemsList = [];
                if (order.itemsJson) {
                  try { itemsList = typeof order.itemsJson === 'string' ? JSON.parse(order.itemsJson) : order.itemsJson; } catch (e) { itemsList = []; }
                }
                if (!Array.isArray(itemsList) || itemsList.length === 0) {
                  itemsList = [{
                    stockCode: order.stockItem ? order.stockItem.stockCode : '-',
                    name: order.stockItem ? order.stockItem.name : 'Ürün Kalemi',
                    productName: order.stockItem ? order.stockItem.name : 'Ürün Kalemi',
                    quantity: order.quantity,
                    unit: order.stockItem ? order.stockItem.unit : 'Adet',
                    unitPrice: order.unitPrice,
                    discountRate: order.discountRate || 0,
                    vatRate: order.taxRate || 20,
                    subtotal: order.subtotal,
                    totalAmount: order.totalAmount
                  }];
                }
              %>
                <tr>
                  <td><strong style="color: var(--indigo); font-family: 'JetBrains Mono', monospace; font-size: 0.825rem;"><%= order.orderNo %></strong></td>
                  <td>
                    <div style="font-weight: 600;"><%= order.supplierName %></div>
                    <% if (order.supplierTaxNo) { %>
                      <div style="font-size: 0.75rem; color: var(--text-muted);">VN: <%= order.supplierTaxNo %></div>
                    <% } %>
                  </td>
                  <td>
                    <div style="display: flex; flex-direction: column; gap: 4px;">
                      <% itemsList.forEach(it => { %>
                        <div style="font-weight: 500; color: var(--text-primary); font-size: 0.875rem;"><%= it.productName || it.name || (order.stockItem ? order.stockItem.name : 'Ürün Kalemi') %></div>
                      <% }); %>
                    </div>
                  </td>
                  <td>
                    <div style="display: flex; flex-direction: column; gap: 4px; font-family: 'JetBrains Mono', monospace; font-size: 0.825rem; color: var(--text-secondary);">
                      <% itemsList.forEach(it => { %>
                        <div><%= parseFloat(it.quantity || 1).toLocaleString('tr-TR') %> <%= it.unit || (order.stockItem ? order.stockItem.unit : 'Adet') %></div>
                      <% }); %>
                    </div>
                  </td>
                  <% 
                    let totalDiscountVal = parseFloat(order.discountAmount) || 0;
                    if (totalDiscountVal === 0 && itemsList.length > 0) {
                      itemsList.forEach(it => {
                        const q = parseFloat(it.quantity) || 1;
                        const p = parseFloat(it.unitPrice) || 0;
                        const d = parseFloat(it.discountRate) || 0;
                        totalDiscountVal += (q * p) * (d / 100);
                      });
                    }
                  %>
                  <td style="font-family: 'JetBrains Mono', monospace; font-size: 0.85rem; color: #fb7185; font-weight: 600;">
                    <% if (totalDiscountVal > 0) { %>
                      - <%= totalDiscountVal.toLocaleString('tr-TR', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %> <%= order.currency %>
                    <% } else { %>
                      <span style="color: var(--text-muted); font-size: 0.8rem;">-</span>
                    <% } %>
                  </td>
                  <td><strong style="color: var(--emerald); font-family: 'JetBrains Mono', monospace;"><%= parseFloat(order.totalAmount || 0).toLocaleString('tr-TR', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %> <%= order.currency %></strong></td>
                  <td style="font-size: 0.8rem;">
                    <%= order.paymentTerm === 'Pesin' ? 'Peşin' : order.paymentTerm === 'Vadeli_30' ? '30 Gün Vadeli' : order.paymentTerm === 'Vadeli_60' ? '60 Gün Vadeli' : order.paymentTerm === 'Vadeli_90' ? '90 Gün Vadeli' : 'Kredi Kartı' %>
                  </td>
                  <td style="font-size: 0.8rem; color: var(--text-muted);"><%= new Date(order.orderDate).toLocaleDateString('tr-TR') %></td>
                  <td>
                    <% if (order.status === 'Received') { %>
                      <span class="status-pill status-active">● Teslim Alındı</span>
                    <% } else if (order.status === 'Ordered') { %>
                      <span class="status-pill status-approved">● Sipariş Verildi</span>
                    <% } else if (order.status === 'Pending_Approval') { %>
                      <span class="status-pill status-pending">● Onay Bekliyor</span>
                    <% } else if (order.status === 'Partial_Received') { %>
                      <span class="status-pill status-onleave">● Kısmi Teslim</span>
                    <% } else if (order.status === 'Cancelled') { %>
                      <span class="status-pill status-rejected">● İptal</span>
                    <% } else { %>
                      <span class="status-pill status-pending">● <%= order.status %></span>
                    <% } %>
                  </td>
                  <td>
                    <a href="/purchase/orders/<%= order.id %>/edit" class="btn btn-sm btn-secondary" style="white-space: nowrap;">📋 Gör / Form</a>
                  </td>
                </tr>
              <% }); %>
            <% } %>
          </tbody>
        </table>
      </div>
    </main>
  </div>

  <script>
    function updateClock() {
      const now = new Date();
      const el = document.getElementById('liveTime');
      if (el) el.textContent = now.toLocaleDateString('tr-TR', { day: 'numeric', month: 'long' }) + ' • ' + now.toLocaleTimeString('tr-TR', { hour: '2-digit', minute: '2-digit' });
    }
    updateClock();
    setInterval(updateClock, 1000);
  </script>
</body>
</html>
